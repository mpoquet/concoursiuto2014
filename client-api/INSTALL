Pour python:
    - Installer python-dev ou python3-dev

Pour Java:
    - Changer le chemin dans le makefile

Compilation (fonctionne que sous linux):
    make spnl all

Penser à mettre à jour la lib quelque fois via :
    git submodule -q foreach git pull -q origin master
